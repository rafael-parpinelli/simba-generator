# Gerador SIMBA CashWay

Aplicação desktop para geração de arquivos SIMBA (MPF),
compatível com múltiplos bancos de dados PostgreSQL.

## Requisitos
- Python 3.11
- macOS (arm64)
- PostgreSQL

## Setup
```bash
python3 -m venv venv311
source venv311/bin/activate
pip install -r requirements.txt
